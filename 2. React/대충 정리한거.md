리액트는 CSR 기반의 라이브러리이다.
리액트는 컴포넌트 기반의 라이브러리이다.
리액트는 가상 DOM을 사용하여 실제 DOM을 조작하지 않고, 가상 DOM을 사용하여 실제 DOM을 조작한다.
리액트는 Props를 통해 단방향 데이터 흐름을 가진다.
리액트는 데이터를 State(내부 상태)와 Props(외부로 부터 전달받은 상태)를 나타내는 render가 있다.
리액트는 상태가 변경될때마다 re-render되고 실제로 변경된 부분만 화면에 업데이트 된다.
